# Parallel Conversation-Tool Groups (Tabbed UI) + Per-Round Assistant Message Boxes — Plan

| | |
|---|---|
| **Author** | Rovo Dev |
| **Date** | 2026-06-19 |
| **Status** | v2 (uncommitted) — supersedes v1 |
| **Branch** | `dev_xinli_2601` |
| **Motivating session** | `OpenStartup/.../server_20260619_033151_b83b9f85/.../turn_002` (Model Optimization SOP Phase 0a) |
| **Sibling plans** | `typed_input_and_composite_choice_plan.md`, `codex/conditional_path_inputs_plan.md`, `codex/parallel_groups_assistant_rounds_turn_artifacts_plan.md` |
| **v1 backup** | `.parallel_tool_groups_and_per_round_message_boxes_plan.v1.bak` (267 lines) |

## 🚨 REVIEWER BANNER — v1 → v2 (cross-plan integration; 3 corrections, 1 NEW sub-problem)

A peer-plan audit (Claude's `update-…-goose.md` — now on-topic and the strongest of the three; Codex's `codex/parallel_groups_assistant_rounds_turn_artifacts_plan.md` — conceptually good but **zero file:line citations**) surfaced findings my v1 got wrong or missed. All re-verified against live source:

* **CORRECTION (Problem A) — a tabbed widget ALREADY EXISTS; v1 was wrong to "build a new one".** `MultiInputWidget.js` (in `react-shared/src/inputs/`) is a tabbed compound widget that already exists — but it is **unreachable** (the dispatcher never routes to it) **and lossy** (it flattens `{choice_index, inputs}` to a scalar at `MultiInputWidget.js:59-69` and emits `{values}` at `:93`, dropping the typed-input plan's composite nested payload). The real blocker is `ConversationToolWidget.js:117` — a **hardcoded `if (metadata.compound) → CompoundWidget`** (the sequential "Step N of M" stepper at `:61`) that intercepts BEFORE the `widget_type` registry lookup at `:134`. **Fix = rewire the compound branch to a de-lossified `MultiInputWidget` (preserving raw child payloads), NOT build a new `CompoundTabbedInput`.** (Verified: ConversationToolWidget.js:61,117,134; MultiInputWidget.js:4,54-69,93.)
* **NEW SUB-PROBLEM C-2 (both my v1 AND Codex missed it).** The assistant response is **never persisted as its own message**, and the session index goes stale: `data_svc.append_message` (used by the WS route at `manager_websocket_routes.py:374`) → `session_store.append_message:334-345` calls `_persist_session` but **NOT `_update_index()`** → `sessions_index.message_count` stays `1` (this is the "1 msgs" you saw) while the header counted 3. Also `run_conversation_turn` blocks for the whole loop and appends **once** with `final_content` (`manager_websocket_routes.py:423,429`) — intermediate rounds never become their own messages. (Verified: session_store.py:334-345; data_service.py:670-671; manager_websocket_routes.py:374,423,429.)
* **MODEL UPGRADE — adopt the turn/round hierarchy.** v1's Problem-C fix was "remove the +1 anchor". The cleaner, more elegant model (from Claude's plan, verified compatible) is: **Turn = one user message + its complete response = one `turn_NNN/`; Round = one LLM iteration within a turn = `turn_NNN/round_MMM/`.** The AF `on_new_turn` callback is really a **round boundary**; OpenStartup must reinterpret it as "new round within the current user turn", not "new top-level turn". This single seam unifies B (per-round bubble), C-1 (no dir split), and C-2 (persist each round) — see §D7 (rewritten).

Everything in v1 that was correct (Problem B's "no `message_id`" diagnosis; the `parallel_group` int-not-bool contract; fix order B+C before A) survives. Full trail in §A4 v2 changelog; §A4.1 answers "pick one plan".

---

## §0 Quick-start (TL;DR for a reviewer)

Two **independent** UI problems observed in one human-test turn, each with a **source-verified root cause**:

- **Problem A — "tools render one-by-one, not as tabs."** When the LLM emits multiple conversation tools, we want them shown together in a **tabbed** widget. The server *already* builds a `compound` widget for tools emitted in a single round, but **(1)** no React component renders `compound` as tabs, and **(2)** there is no `parallel_group` field, so the LLM cannot express which tools may be shown together vs. must run sequentially.
- **Problem B — "the second assistant response overwrites the first."** Within one turn the LLM produced two assistant rounds; the second replaced the first on screen. **Root cause:** there is **no per-round message identity** end-to-end — the server emits `message_start`/`token`/`message_end`/`pending_input` with **no `message_id`**, so the UI cannot keep two rounds as two distinct bubbles.

This plan fixes **B first** (it's a correctness bug and a prerequisite — without per-round boxes, tabbed groups still can't coexist with preceding text), then **A** (the `parallel_group` feature).

- **Problem C — "one user request shows as two turns."** A single user message produced **two** `turn_NNN/` directories: `turn_001/` holds the user input + (empty) prompt scaffolding but **no response rounds**, and `turn_002/` holds the assistant's response rounds but **no user input**. **Root cause:** the cache-folder rotation advances the turn directory **per agentic-loop iteration**, not **per user turn** — and uses a different anchor (`new_turn + 1`) than `save_turn_data` (`new_turn`), so the user input and its own response are split across two directories. The UI counts `turn_` directories, so it reports "2 turns" for 1 exchange. **Same root family as B** (loop-iteration boundary conflated with conversational-turn boundary). Fixed in §E1.8–§E1.9.

---

# PART I — EXECUTION

> Order: **Commit 0 (preflight) → Commits 1–3 (Problem B) → Commits 8–9 (Problem C) → Commits 4–7 (Problem A)**. B+C are correctness bugs and share a root family (turn/round boundary confusion); A is the new feature built on top. B and C can proceed in parallel (different files: B = emit/UI, C = conversation_service); A depends on both.

## §E1 Commits

### §E1.0 Commit 0 — Preflight source locks (~0 LoC, verification only)

Before editing, confirm the live line anchors (they drift): `agent_service_bridge.py` emit sites for `message_start`/`token`/`message_end`/`pending_input`; `useManagerChat.js` cases `token`(:224), `message_start`(:259), `pending_input`(:266), `message_end`(:357); `conversational_inferencer.py` compound build (:2310-2390) and single-tool emit (:1977-2127). Record actual line numbers in the PR description.

### §E1.1 Commit 1 — Server: mint + propagate `message_id` per round (Problem B)

**Files:**
1. `…/conversational/conversational_inferencer.py` — at the start of each assistant round (where `message_start` is triggered / the streaming target is set up), mint `self._current_message_id = uuid.uuid4().hex[:12]`. Attach it to the response objects that become `message_start`, `token`, `message_end`, and `pending_input`.
2. `…/ui/webui/backend/routes/agent_websocket_routes.py` — include `message_id` in the `{"type":"message_start"}` emits (:319,:360).
3. `…/ui/webui/backend/services/agent_service_bridge.py` — copy `message_id` from the polled `resp` onto the `token`(:181), `message_end`(:191,:298,:336), and `pending_input`(:235) emits (mirroring how `turn_number`/`task_id` are already copied at :196-197).

**Tests:** `test_message_id_per_round.py` — a two-round turn produces two distinct `message_id`s across the emitted `message_start`/`message_end` events; a single-round turn produces one.

**Risk:** Low/additive (R1). `uuid` import already present in the inferencer (used elsewhere).

### §E1.2 Commit 2 — UI: key streaming bubbles by `message_id` (Problem B)

**File:** `OpenStartup/.../ui/src/hooks/useManagerChat.js`

**Changes:**
1. Track `currentMessageIdRef`. On `message_start` with a new `message_id`: **commit any in-progress streaming bubble first** (so round 1's text is preserved), then open a fresh streaming buffer for the new id.
2. `token`: append to the buffer for `data.message_id` (fallback: current).
3. `message_end` / `pending_input`: commit/finalize the bubble for `data.message_id`; set its committed `id = data.message_id` (replaces the `Date.now()` fallback at :384).
4. Dedupe guard (R2): a `message_id` already committed is never re-appended.

**Tests (RTL):** `useManagerChat.bubbles.test.js` — simulate `message_start(id=A)`, tokens, `message_end(A)`, then `message_start(id=B)`, tokens, `pending_input(B)`. Assert **two** bubbles exist with contents A and B (regression for the overwrite bug).

### §E1.3 Commit 3 — E2E regression for Problem B

Reproduce the turn_002 scenario (text round then tools round) in an integration test; assert the committed message list has both the preamble text bubble and the tools bubble. Lock the fix.

### §E1.4 Commit 4 — Parser + model: `parallel_group` field (Problem A)

**Files:**
1. `…/conversational/conversation_response_parser.py` — read optional `parallel_group: int` from each tool object; default `None`. Preserve on the parsed tool.
2. `…/conversational/conversation_tools.py` — add `parallel_group: int | None = None` to `ConversationTool` (+ `from_dict`/`to_dict`).

**Tests:** parse a 2-tool block with explicit `parallel_group: 0` on both → both carry group 0; omitted → `None` (implicit-coalesce handled in Commit 5).

### §E1.5 Commit 5 — Server: group tools + signal tabbed widget (Problem A)

**File:** `…/conversational/conversational_inferencer.py` (the `_handle_conversation_tools` build, :2310-2390).

**Changes:**
1. Partition emitted tools into ordered groups: explicit `parallel_group` values define groups; tools with `None` coalesce with adjacent `None` tools (§D3 default).
2. For each group in order: emit one round (its own `message_id` from Commit 1). If the group has >1 tool, build the existing `compound` metadata **plus** `metadata["widget_type"] = "compound_tabbed"`. Single-tool group → existing single widget.
3. Sequential groups: resolve group N's `pending_input` before emitting group N+1 (the loop already awaits interaction; iterate groups).

**Tests:** `test_tool_grouping.py` — (a) two tools same group → one compound_tabbed round; (b) two tools different groups → two sequential rounds; (c) omitted groups → one coalesced round; (d) every group's `output_vars` get bound via the compound collector.

### §E1.6 Commit 6 — UI: rewire + de-lossify the EXISTING `MultiInputWidget` (Problem A) **[v2 REWRITE]**

**🚨 v2 CORRECTION.** v1 said "build a new `CompoundTabbedInput`". Source check proved a tabbed widget **already exists** (`MultiInputWidget.js`) but is (a) **unreachable** — `ConversationToolWidget.js:117` hardcodes the compound branch to the sequential `CompoundWidget` stepper before the registry lookup — and (b) **lossy** — `MultiInputWidget.js:59-69` flattens `{choice_index, inputs}` to a scalar and `:93` emits `{values}`, dropping composite payloads. Fix = **rewire + de-lossify**, not rebuild.

**Files:**
1. `…/react-shared/src/protocol/ConversationToolWidget.js` — change the hardcoded compound branch (`:117`) so that when the group is parallel/tabbed (>1 tool), it routes to `MultiInputWidget` instead of the `CompoundWidget` stepper. Keep `CompoundWidget` (stepper) available for explicitly-sequential groups (distinct `parallel_group`s rendered one round at a time — but those are separate rounds, so the stepper may be retired entirely; decide in impl, default to tabbed).
2. `…/react-shared/src/inputs/MultiInputWidget.js` — **de-lossify**: preserve each child widget's **raw** response payload under its `output_var` (do NOT scalarize `{choice_index, inputs}`); emit a per-output-var map `{outputs: {<var>: <raw>}}` (compatible with the server compound collector + `finalize_input_value`), not the lossy `{values}`. Keep tab state across switches; enable Submit only when all required children are complete.

**Tests (RTL):** renders N tabs (not "Step N of M"); switching tabs preserves per-tab input; a composite single_choice tab returns its full `{choice_index, inputs}` (regression for the scalarization bug); Submit returns the per-output-var map covering all tools; a 1-tool group renders without tabs.

### §E1.7 Commit 7 — Response-Format guidance + Phase 0a SOP example (Problem A)

**Files:**
1. The conversation Response-Format template (`conversation/main/initial.jinja2` or the ResponseSchema doc block) — document `parallel_group` with one example (the Phase 0a path + artifacts pair as group 0).
2. `model_optimization/SOP.md` Phase 0a — annotate the two tools with `parallel_group: 0` so they render tabbed (optional; the implicit-coalesce default already groups them, but the explicit example is instructive).

### §E1.8 Commit 8 — Server: turn/round hierarchy (Problem C-1) **[v2 REWRITE — foundation]**

**File:** `OpenStartup/.../server/services/conversation_service.py` (+ entrypoint in `manager_websocket_routes.py`).

**Changes (per §D7 model):**
1. **C-1a anchor:** stop rotating `cache_folder` to `turn_{new_turn + 1}` (line 624). The **turn** dir is stable for the whole user message; rounds rotate within it.
2. **C-1b granularity:** increment the **turn** number **once per user message** at the `run_conversation_turn` entrypoint. Reinterpret the per-iteration `_on_new_turn` as a **round** boundary: rotate `cache_folder` to `turn_{turn}/round_{round_index:03d}/`, not a new top-level turn.
3. Nest round artifacts under `turn_NNN/round_MMM/`. Keep `turn_NNN/turn.json` (user input + final assembled response) at the turn level.
4. Reconcile `initial_turn` counting (line 506) so a fresh session's first user message yields exactly one `turn_001/`.

**Tests:** `test_turn_round_hierarchy.py` — a single user message triggering a widget handoff (≥2 rounds) creates exactly **one** `turn_001/` containing `turn.json` AND nested `round_001/`, `round_002/`; UI turn-count (dir count, C-F3) == 1.

**Risk:** Medium — `get_turn_data` (session_routes.py:148) + View-Prompt resolve `turn_NNN/`. Assert `GET /{sid}/turns/1` still returns the turn; add round-aware resolution for View-Prompt.

### §E1.9 Commit 9 — E2E regression for Problem C-1

Replay the scenario (one message → SOP-enter round → Phase 0a widget round). Assert: exactly one `turn_001/` with `turn.json` + nested `round_*`; UI turn count == 1. Lock the fix.

### §E1.10 Commit 10 — Server: persist each round as a message + fix the index count (Problem C-2) **[NEW in v2]**

**Files:**
1. `OpenStartup/.../server/services/session_store.py` — `append_message` (`:334-345`) must call `_update_index()` after `_persist_session` so `sessions_index.message_count` stays correct (C-2b).
2. `OpenStartup/.../server/routes/manager_websocket_routes.py` — at each round boundary (the §D7 seam) AND at loop end, append the round's assistant text as its **own** message tagged `{turn_number, round_index, message_id}` (C-2a), instead of only one `final_content` at `:469`.
3. Ensure the round-boundary persistence and the WS `message_end`/`message_start` framing (Commit 1) fire from the **same** seam so persisted messages and UI bubbles stay 1:1.

**Tests:** `test_round_persistence_and_count.py` — a 2-round turn persists **2** assistant messages (each with distinct `message_id`/`round_index`); `sessions_index.message_count` equals the true message count (regression for the stale "1 msgs" symptom, C2-F3); a widget-pending round persists its text before the widget resolves.

**Risk:** Medium — touches the persistence path; guard against double-persist (a round committed at its boundary must not be re-appended at loop end). Mirrors the UI double-commit guard in Commit 2 (R2).

## §E2 Validation

- Unit: Commits 1,2,4,5,6 tests above.
- E2E: Commit 3 (Problem B regression) + a Problem A E2E (two same-group tools → tabbed widget, both vars bound).
- Manual: re-run the Model Optimization SOP; confirm (B) the SOP-enter text and the Phase 0a tools appear as **two** assistant boxes, and (A) the two Phase 0a tools appear as **tabs** within the second box.

## §E3 Execution checklist

* [ ] **Commit 0** — relock all line anchors; record in PR.
* [ ] **Commit 1** — server `message_id` minting + propagation (inferencer, websocket_routes, service_bridge) + test.
* [ ] **Commit 2** — UI bubble keying by `message_id` + dedupe + RTL test.
* [ ] **Commit 3** — Problem B E2E regression.
* [ ] **Commit 4** — parser + model `parallel_group` + test.
* [ ] **Commit 5** — server grouping + `compound_tabbed` signal + tests.
* [ ] **Commit 6 (v2 rewrite)** — rewire `ConversationToolWidget` compound branch → de-lossified `MultiInputWidget` (tabs) + RTL tests (incl. composite-payload regression).
* [ ] **Commit 7** — Response-Format guidance + Phase 0a example.
* [ ] **Commit 8 (Problem C-1, v2 rewrite)** — turn/round hierarchy: stable turn dir, `_on_new_turn` → round boundary, `turn_NNN/round_MMM/` + test.
* [ ] **Commit 9 (Problem C-1)** — E2E regression (one message → one turn_001 with nested rounds; turn-count == 1).
* [ ] **Commit 10 (Problem C-2, NEW)** — persist each round as its own message + `append_message` calls `_update_index()` + test (true `message_count`).
* [ ] **Pre-flight before coding:**
  - [ ] P1: confirm `uuid` is imported in `conversational_inferencer.py` (Commit 1).
  - [ ] P2: confirm the per-round boundary site (where `message_start` originates) so `message_id` is minted exactly once per round.
  - [ ] P3: confirm the compound submit payload shape the UI sends today (`{outputs:{...}}` vs other) so Commit 5's collector reads the right keys (ties to typed-input plan R-D7).

---

# PART II — DESIGN & RATIONALE

## §D1 Problem B — per-round message identity (the model)

**Decision:** the *round* (one assistant streaming turn between user/tool interactions) is the unit of a UI bubble. Each round gets a server-minted `message_id` (uuid4 hex, short). This id is the single source of truth for "which bubble does this content belong to".

**Why server-minted, not UI-derived:** only the server knows where a round begins (it controls the agentic loop and the `message_start` emit). A UI heuristic ("new bubble when content resets") is fragile — it already failed here (B-F5). The server emitting the boundary id is the correct, deterministic design.

**Lifecycle:**
```
round begins  → server: message_start {message_id}
streaming     → server: token {message_id, content}
round ends:
  • normal    → server: message_end {message_id, final_content, turn_number}
  • needs input → server: pending_input {message_id, input_mode, ...}
```
The UI keeps a `currentMessageId`. On `message_start` it opens a new streaming bubble keyed by that id. `token` appends to the bubble with the matching id. `message_end`/`pending_input` commit/finalize that id's bubble. A subsequent `message_start` with a **new** id opens a **new** bubble — round 1's bubble is already committed and untouched.

## §D2 Problem B — why this also unblocks Problem A

A tabbed group (Problem A) is attached to a `pending_input` round. If that round shares a buffer with the preceding text round (today's bug), the text round's content is lost when the tabbed widget arrives. **Per-round identity (B) is therefore a prerequisite for A** — only once each round is its own bubble can "preamble text bubble" + "tabbed-tools bubble" coexist. This is why the plan does B first.

## §D3 Problem A — `parallel_group` semantics (the contract)

A new optional integer field on each emitted tool: `parallel_group`.

- **Same `parallel_group` value** ⇒ those tools may be shown **together** (one tabbed compound widget) and are answered in one submit.
- **Different values** ⇒ tools run **in sequence** (group 0 fully resolved before group 1 is shown), each as its own round.
- **Omitted `parallel_group`** ⇒ **consecutive tools coalesce** into one implicit group (the friendly default — improves existing SOPs without edits). The moment a tool declares an explicit group, the explicit grouping takes over for that response.

This is purely *expressive*: it lets the SOP author say "ask these two questions side-by-side" vs "ask Q1, then based on the answer, ask Q2".

**Why integer, not bool:** a bool ("parallel: true") can't express *more than one* sequential group in a single response (e.g. group 0 = [path, artifacts], then group 1 = [confirm]). An ordered integer can.

## §D4 Problem A — rendering (the widget) **[v2 REWRITE]**

**v2 correction:** a tabbed widget already exists (`MultiInputWidget.js`) but is unreachable + lossy (banner). So the fix is **rewire + de-lossify**, not build new.

- The blocker is `ConversationToolWidget.js:117` — a hardcoded `if (metadata.compound) → CompoundWidget` (the "Step N of M" stepper at `:61`) that runs **before** the `widget_type` registry lookup (`:134`). Change this branch so a parallel/tabbed group (>1 tool) routes to `MultiInputWidget` (tabs).
- **De-lossify `MultiInputWidget`** (`:59-69`, `:93`): preserve each child widget's **raw** response under its `output_var` (do NOT scalarize `{choice_index, inputs}`), and emit `{outputs: {<var>: <raw>}}` — compatible with the server compound collector + `finalize_input_value`. This is what lets composite single_choice (the typed-input plan's nested-input) survive through a tab.
- Each tab embeds the *existing* per-tool widget (clarification → TextInput/PathAutocomplete; single_choice → SingleChoiceWidget). `MultiInputWidget` is a *container*. A single-tool group renders without tabs.
- Server: when a group has >1 tool, keep emitting `metadata.compound = true` (already done) and additionally set a clear tabbed signal the rewired branch reads (e.g. `metadata.layout = "tabbed"`); sequential groups (distinct `parallel_group`s) are simply separate rounds, so the stepper can be retired.

**Reuse, not reinvent:** composes cleanly with the typed-input plan's `PathAutocompleteInput`/`MultiValueInput` (those render inside each tab).

## §D5 Problem A — submission & binding

The compound submit returns `{ outputs: { <output_var>: <value>, ... } }` covering every tab. The server's existing compound collection (`_handle_conversation_tools`, the 2205-2323 block) already iterates tools and binds `output_vars`; it routes each value through `finalize_input_value` (from the typed-input plan) — so multi-value/path/composite semantics are handled in one chokepoint (R4). No new binding logic; we extend the existing compound collector to read the per-tab `outputs` map.

## §D6 Verified facts (consolidated)

See §A1. The load-bearing facts: **B** = no `message_id` anywhere in the emit hub (B-F2/F3/F4); **A** = compound metadata exists server-side for same-round multi-tool (A-F1) but no UI renderer (A-F4) and no grouping field (A-F3); **C** = turn dir advances per loop-iteration with a `+1` anchor mismatch (C-F4/F5/F6/F7).

## §D7 Problem C — the turn/round hierarchy (fixes C-1 + C-2, unifies with B)

**The model (the single foundation for B, C-1, C-2):**

- **Turn** = one user message + the assistant's complete response. **One `turn_NNN/` directory.** The turn number increments **once per user message** (at the `run_conversation_turn` entrypoint), never per round.
- **Round** = one LLM iteration within a turn. A turn has ≥1 rounds, nested as **`turn_NNN/round_MMM/`** (round artifacts + stream cache). The AF `on_new_turn` callback is really a **round boundary**; OpenStartup reinterprets it as "new round within the current user turn", not "new top-level turn".
- Each round that produces assistant text → **its own UI bubble (B)** AND **its own persisted assistant message (C-2)**, tagged `{turn_number, round_index, message_id}`. A round's `pending_input` belongs to that round.
- The **round-boundary seam** is the single hook that unifies everything: on each boundary, (i) rotate the cache to the next `round_MMM/`, (ii) persist the just-finished round's assistant text as a message (C-2), (iii) emit a WS frame (`message_end` of the finished round then `message_start` of the next, each with its `message_id`) so the UI commits that round's bubble (B).

**The fixes:**

1. **C-1a (anchor mismatch):** stop rotating `cache_folder` to `turn_{new_turn+1}` (conversation_service.py:624). Rounds rotate within `turn_NNN/round_MMM/`; the **turn** dir is stable for the whole user message.
2. **C-1b (granularity):** the turn number advances **once per user message** at the `run_conversation_turn` entrypoint, not on the inferencer's per-iteration `_on_new_turn`. `_on_new_turn` becomes a **round** boundary (rotate `round_MMM`, persist round, emit WS frame).
3. **C-2a (persist each round):** at each round boundary and at loop end, append the round's assistant text as its **own** message (`role:"assistant"`, `turn_number`, `round_index`, `message_id`), instead of only one `final_content` at the very end (manager_websocket_routes.py:469).
4. **C-2b (fix the count):** make `session_store.append_message` call `_update_index()` (session_store.py:334-345) so `sessions_index.message_count` stays correct.

**Backward-compat:** keep `turn_NNN/` as the path the prompt-viewer/`get_turn_data` (session_routes.py:148) resolves, but its contract tightens to "user turn", with rounds nested beneath. View-Prompt opens the correct `turn_NNN/round_MMM/`.

This is the elegant unification: one user message ⇒ one `turn_NNN/` ⇒ N `round_MMM/` ⇒ N assistant bubbles (B) ⇒ N persisted messages (C-2) ⇒ correct index count (C-2b) ⇒ UI turn-count = 1 (C-F3).

---

# APPENDIX

## §A1 Verified root causes (source-anchored)

### Problem B — second response overwrites first

| # | Fact | Source |
|---|---|---|
| B-F1 | The turn produced **two** assistant rounds on disk (one "enter SOP" text round at 203746, one Phase-0a-with-two-tools round at 203807). Both exist; only the second shows in the UI. | the turn_002 directory listing |
| B-F2 | The server emit hub emits token, message_end, pending_input with **NO message_id** field. grep for message_id in the bridge returns zero hits. | agent_service_bridge.py:84,122,181,191,235,298,336 |
| B-F3 | message_start carries **no id** either — emitted bare as type-only. | agent_websocket_routes.py:319,360 |
| B-F4 | The UI commits a finished bubble at message_end with id falling back to a timestamp when data.message_id is absent — i.e. it **expects** a server message_id but the server never sends one. | useManagerChat.js:384 |
| B-F5 | The streaming buffer is a **single shared ref** (streamingContentRef.current); message_start resets it to empty (:260). A round ending via pending_input only commits its text if non-empty (:270), and the next round's message_start then clears the buffer. No per-round bubble survives across rounds. | useManagerChat.js:60,238,260,269-292 |
| B-F6 | message_end (:359) and pending_input are keyed only by turn_number/task_id — both **stable within a turn** — so two rounds in one turn cannot be distinguished by the UI. | useManagerChat.js:159-160,358-359; agent_service_bridge.py:196-197 |

**Conclusion (B):** the bug is the **absence of a per-round message identity** propagated server→UI. Fix: mint a message_id per assistant round on the server, attach it to message_start/token/message_end/pending_input, and have the UI key each streaming bubble by message_id (new bubble when the id changes). Root-cause fix — not a UI-only patch that guesses round boundaries.

### Problem A — multiple tools not shown as tabs

| # | Fact | Source |
|---|---|---|
| A-F1 | When >1 tool is emitted **in one round**, the server already builds a single compound widget: InputModeConfig(mode=FREE_TEXT, metadata={compound: True, tools: tool_configs}). | conversational_inferencer.py:2373-2378 |
| A-F2 | Each tool_configs[i] carries tool_type, prompt, input_mode, output_var, expected_input_type, prefix. Order preserved (list). | conversational_inferencer.py:2362-2369 |
| A-F3 | The parser preserves tool order but has **no parallel_group/group field** today. | conversation_response_parser.py (grep parallel_group/group → none) |
| A-F4 | There is **no React component** that renders metadata.compound===true as tabs (grep compound/Tabs in chat UI → only unrelated MUI Tabs in popovers). | OpenStartup/.../ui/src grep |
| A-F5 | The widget dispatcher routes by metadata.widget_type or mode (precedent: proposal_selection, confirmation, path_input). A compound/tabbed widget can follow the same widget_type dispatch. | typed-input plan §D6 F10; registerBuiltins.js |

**Conclusion (A):** tabbed rendering is **partially server-ready** (compound metadata exists for same-round multi-tool) but (1) the UI never renders it as tabs, and (2) the LLM cannot *express* grouping intent. Feature needs: a parallel_group field (parser + model), server grouping logic, a widget_type "compound_tabbed" signal, and a React tabbed widget.

### Problem C — one user request becomes two turn directories

| # | Fact | Source |
|---|---|---|
| C-F1 | `turn_001/turn.json` holds `user_input: "help improve the model for responsible ai"` but `rendered_prompt: ""` and **no `RovoDevCliInferencer/` rounds**. | turn_001/turn.json |
| C-F2 | `turn_002/` holds **two** `RovoDevCliInferencer-…` round dirs (203746, 203807) but **no `turn.json`/user_input**. | turn_002 listing |
| C-F3 | The UI numbers turns by **counting `turn_` directories**: `new_style = sum(1 for p in session_dir.iterdir() if p.name.startswith("turn_") …)`. Two dirs → "2 turns". | conversation_service.py:506-508 |
| C-F4 | `save_turn_data` writes the user input under **`turn_{new_turn}`** (e.g. turn_001). | conversation_service.py:~610 (step 2) |
| C-F5 | The streaming `cache_folder` is rotated to **`turn_{new_turn + 1}`** (e.g. turn_002) — a **different anchor** for the same turn. | conversation_service.py:624 (step 4) |
| C-F6 | `_on_new_turn` (which runs step 2 + step 4) fires **per agentic-loop boundary**, including a **pre-loop** call at `start_iteration==0` and again after **each widget handoff** — NOT once per user message. | conversational_inferencer.py:371 (pre-loop), :568 (post-widget) |
| C-F7 | The pre-loop `cache_folder` is set to `turn_{initial_turn+1}` (turn_001) at line 633, but the immediately-following pre-loop `_fire_new_turn` (line 371) runs step 4 and **repoints it to turn_002** — so the next iteration of the SAME response streams into turn_002. | conversation_service.py:633; conversational_inferencer.py:371; conversation_service.py:624 |

**Conclusion (C-1):** two compounding bugs. **C-1a (off-by-one anchor):** `save_turn_data(new_turn)` and `cache_folder(new_turn+1)` disagree, so a turn's user input and its response land in different directories. **C-1b (granularity):** the turn directory advances per agentic-loop iteration (incl. the pre-loop init and every widget handoff), not per user message — conflating "loop iteration" with "conversational turn" (the same root family as Problem B). Fix: advance the turn directory **once per user message**, and make the response co-locate with its own user input (same anchor).

### Problem C-2 — assistant response never persisted as a message + stale index (NEW in v2; both v1 and Codex missed it)

| # | Fact | Source |
|---|---|---|
| C2-F1 | The WS route appends the assistant message **once** at loop end via `final_content` (built by blocking on `run_conversation_turn`); intermediate rounds are never persisted as messages. | manager_websocket_routes.py:423,429,469 |
| C2-F2 | `session_store.append_message` (used via `data_svc.append_message`, data_service.py:670-671) calls `_persist_session` but **NOT `_update_index()`** — so `sessions_index.message_count` goes stale. | session_store.py:334-345 (no `_update_index`); session_store.py:681 (where `_update_index` lives) |
| C2-F3 | Symptom: the session list showed **"1 msgs"** (stale index count) while the chat header showed "3 messages" (welcome + user + assistant) — exactly the staleness predicted by C2-F2. | observed UI + session_state.json |

**Conclusion (C-2):** (a) persist each round's assistant text as its own message at the round boundary (not one `final_content` at the very end), tagged `{turn_number, round_index, message_id}`; (b) make `append_message` call `_update_index()` so the index stays correct. C-2 is why the assistant turn appeared to "not exist" in the session list even though it streamed on screen.

## §A2 Follow-ups (out of scope here)

- **F-consolidate:** dormant handler-registry vs. live inline _build_input_mode split (tracked in typed-input plan §A4.1) — this plan also touches the live inline path; consolidation remains separate.
- **Concurrent interactive tabs with independent submit:** this plan renders grouped tools in tabs but submits them as one compound payload. True per-tab async submit is deferred.

## §A3 Risks

| # | Risk | Mitigation |
|---|---|---|
| R1 | Adding message_id changes the WS contract; an out-of-date UI would ignore it | message_id is additive; UI already falls back to Date.now(). Server stays backward-compatible. |
| R2 | Per-round bubbles could double-render the final message if both pending_input (commit) and message_end fire for the same id | Key both commit paths by message_id; a bubble with a given message_id is committed at most once (UI reducer guard). |
| R3 | parallel_group semantics could confuse the LLM | Explicit Response-Format guidance + examples; default (omitted group) = "consecutive tools may be shown together" so existing SOPs improve without edits. |
| R4 | Tabbed compound submit must still bind every tool's output_var (incl. composite nested inputs from typed-input plan) | Reuse the compound collection + finalize_input_value chokepoint; test that a tabbed group binds all output vars. |

## §A4 Changelog

* **v1 (2026-06-19)** — initial draft. Three problems (A/B/C), source-anchored root causes, fix order B+C→A.
* **v2 (2026-06-19, this document)** — cross-plan integration (Claude's on-topic plan + Codex's `parallel_groups_assistant_rounds_turn_artifacts_plan.md`). Changes:
  * **Problem A corrected:** v1 said "build a new `CompoundTabbedInput`". Source proved a tabbed `MultiInputWidget` **already exists** but is unreachable (hardcoded `CompoundWidget` stepper at `ConversationToolWidget.js:117`, before the registry) and **lossy** (scalarizes `{choice_index,inputs}` at `MultiInputWidget.js:59-69`, emits `{values}` at `:93`). Commit 6 rewritten to **rewire + de-lossify**, not rebuild. (§D4, §E1.6.)
  * **Problem C-2 ADDED (both v1 and Codex missed it):** assistant response never persisted as its own message; `append_message` (`session_store.py:334-345`) skips `_update_index()` → stale `message_count` (the "1 msgs" symptom). New Commit 10. (§A1 C-2 table, §E1.10.)
  * **Model upgraded to turn/round hierarchy** (`turn_NNN/round_MMM/`): `_on_new_turn` reinterpreted as a round boundary; one user message ⇒ one turn ⇒ N rounds ⇒ N bubbles (B) ⇒ N persisted messages (C-2). Commit 8 rewritten. (§D7.)
  * Now **10 commits** (was 9). v1 backed up at `.parallel_tool_groups_and_per_round_message_boxes_plan.v1.bak`.
  * Surviving-correct-from-v1: Problem B's "no `message_id`" diagnosis; `parallel_group` int-not-bool; fix order B+C→A.

### §A4.1 If forced to pick ONE plan among the 3

**Honest answer: Claude's `update-…-goose.md`** — it is the strongest single artifact for *this* topic.

Why:
* **Most complete + most correct.** It alone found **Problem C-2** (assistant message never persisted + stale index) that both my v1 and Codex's plan missed — and it correctly diagnosed Problem A as "a tabbed widget exists but is unreachable + lossy" where my v1 wrongly said "build a new one". Both findings verified against source.
* **Best unifying model.** Its turn/round hierarchy (with `on_new_turn` reinterpreted as a round boundary) is the most elegant foundation; B, C-1, C-2 all hang off one seam.
* **Source-anchored.** Every root cause carries file:line evidence. Codex's plan, by contrast, has **zero file:line citations** (conceptually sound but unverifiable as written) — so despite good ideas (implicit-group default, persist-on-round-boundary), it is not execution-grade on its own.

Caveat (honest): my v1 had better *structure* (3-tier, risk register, granular numbered commits/tests) than Claude's flatter Markdown. **v2 (this document) integrates Claude's superior findings + Codex's good ideas INTO that structure** — so for a document to *execute from*, pick **v2**; for the best independent *analysis*, Claude's plan earned it (it caught what I missed).
