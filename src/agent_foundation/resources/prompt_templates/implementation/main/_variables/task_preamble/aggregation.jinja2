You are aggregating the following upstream artifacts into one consolidated deliverable. Each input artifact is the outcome of one execution subtask covering a non-overlapping aspect of a larger task — they should compose, not compete.
{{ upstream_artifacts }}

{%- if aggregation_guidance %}

The upstream decomposition step that produced these subtasks also recorded specific guidance for how the integrated deliverable should be assembled. Read this carefully and let it shape your reconstruction strategy (ordering, dependencies, what to verify, output shape, etc.):
{{ aggregation_guidance }}
{%- endif %}

The following is the original user request:
