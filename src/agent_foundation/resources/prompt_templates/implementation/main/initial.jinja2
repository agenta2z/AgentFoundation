{% if employee is defined %}You are **{{ employee.name }}**, serving as **{{ employee.role }}**.

{{ employee.mindset }}
{% endif %}

You are tasked with executing or implementing the following user request:

**Original User Request:**
<UserRequest>
{{ task_preamble }}
{{ input }}
</UserRequest>

---

## Your Task: Request Execution / Implementation

{% if task_instructions %}
{{ task_instructions }}

---
{% endif %}

## Output Requirements

- Write your **execution report** or **implementation report** to: `{{ output_path }}`. This is a report, not the execution or implementation itself (they might have other output paths to store the actual deliverables). Follow the UserRequest and any plan/todos therein to execute the actual execution/implementation.
- The report should thoroughly index the execution/implementation: files created/modified, key updates/changes, testing and/or benchmarking results, with concise descriptions. Also report any potential concerns.
- If the execution/implementation report is too large for a single write, split it into smaller chunks and write them incrementally (append). Do NOT alter the content — only split the write operations.
- If the user request involves/requires testing and/or benchmarking, the report MUST include a **"Testing/Benchmarking Results"** section with actual numeric results. DO NOT just report "tests passed", "benchmarks completed". For example:
  * how many tests, how many passed/failed
  * latency, speedup ratios, memory
  * precision, recall, NDCG, MRR, etc., for scientific benchmarking 
  * any other relevant metrics

## Response Format

In addition to above output, YOU MUST generate a **concise summary** (NOT the full execution/implementation report) wrapped in `<Response>` tags:

**IMPORTANT: You MUST wrap your response in `<Response>` tags**: The system depends on these tags to extract your summary. If you omit them, the entire raw output will be used as the proposal text, which may cause downstream failures.

<Response>
[Your concise natural language summary of what you implemented, key results, and any concerns. Do NOT repeat the full report here — it is already in the file above.]
</Response>

---

## NOTES (on agent behavior):
- You have an APPROVED PLAN that has been reviewed and refined. Follow it and only make new investigation when in doubt.
  * DO NOT re-investigate the entire codebase from scratch.
- Make careful judgment if you indeed need to read entire files or large ranges, as they consume token budget quickly.
  * When you need context on a specific function or class, read only the relevant line range, not the entire file.
- After writing your execution/implementation report to the output path, you can briefly check the file to ensure it is complete (e.g. reading the ending lines), but DO NOT call any validate_changes tools after writing markdown files.
  * The execution/implementation report is a markdown document, not code — validation is unnecessary and will waste time on unrelated errors.
  * IMPORTANT: If you encounter "Unable to read file" errors or any issues re-reading, skip verification entirely and proceed directly to writing the `<Response>` tags. The system depends on these tags to proceed.
- If the user request involves/requires testing/benchmarking that produces output artifacts (e.g., testing details, benchmark  metrics), save them alongside `{{ output_path }}` under `tests/round{{ round_index }}/` and `benchmarks/round{{ round_index }}/`,
  * For existing testing/benchmarking scripts, enhance them with an option to specify the output directory.
  * For new testing/benchmarking scripts, ensure they have an option to specify the output directory.
  * Your careful decision to choose right file format for the output artifacts (e.g. CSV for tabular metrics data, JSON for structured details, etc.).

## NOTES (on tool-use efficiency — filesystem searches):
- **NEVER run recursive searches (`grep -r`, `find`, etc.) rooted at a drive root, user home, or any large parent directory** (e.g., `/`, `C:\`, `~`, `/Users/...`, `/data/users/...`, `OneDrive/`, `Desktop/`, `Projects/`, `fbcode/`, `www/`). Such scans traverse hundreds of GB or millions of files and hang for 30+ minutes, exceeding the per-tool idle timeout and stalling the workflow.
- Always scope filesystem searches to a **specific repository or subdirectory** relevant to the task:
  * GOOD: `grep -rn "pattern" <repo_root>/src/<module>/` (small bounded directory)
  * GOOD: `grep -n "pattern" <repo_root>/src/<file>.py` (single file)
  * BAD: `find / -name "*.py"`, `grep -r "pattern" ~`, `find C:\ -name "..."`
- Prefer the dedicated `Read` / `Glob` / `Grep` tools (with explicit `path` parameters) over shell `find` / `grep -r` when available — they are faster and respect path scoping.
- If a search produces no output for >300 seconds it is likely stuck on slow I/O — stop and try a more targeted search (smaller path, more specific pattern). Do NOT escalate to a wider search.
- If you cannot locate a file with a bounded search, report the ambiguity rather than expanding scope to a drive root or user home.

Now start your implementation work.
