{{ file_reading_fallback }} **For the Prior Artifacts/Files specifically** (file referenced under `<PriorVersionArtifact>`, `<PriorImplementation>`, etc.): if file-reading tools reject the path (e.g., "outside workspace"), YOU MUST first try using the command-line / shell tools (e.g. `cat`, `grep`, `sed`, `ls`) to read the actual files before trying other alternatives. If BOTH fail, note the tool and command-line failures and the unreachable path in your Response.