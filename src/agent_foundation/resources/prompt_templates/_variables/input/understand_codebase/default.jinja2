Produce careful, in-depth, comprehensive, structured, machine-followable and human-readable documentation at {{ target_path }}/.ai-employee/docs/codebase/ in reStructuredText format. Include concrete file/module references to actual code throughout the documentation.

After writing the documentation, build a navigable HTML site from the reStructuredText sources using Sphinx (or equivalent) so the documentation is browsable locally.
