You are aggregating the following upstream artifacts into one consolidated artifact. Each input artifact might be independent outcomes of the same process with different perspectives or approaches, or might be outcomes for different subproblems/aspects of the a bigger user request.
{{ upstream_artifacts }}

{%- if aggregation_guidance %}

The upstream decomposition step that produced these artifacts also recorded specific guidance for how the integration/consolidation should be done. Read this carefully and let it shape your reconstruction strategy (ordering, weighting, de-duplication, output shape, etc.):
{{ aggregation_guidance }}
{%- endif %}

{%- if include_iteration_judgment %}
Also make this judgment after integration/consolidation: does the integration and consolidation process show any value? Or all the upstream inputs are mostly identical and the integration adds little information?
{%- endif %}

{%- if include_winner_pick %}
Also make this judgment after integration/consolidation: if you are only allowed to pick one of the provided inputs (rather than being asked to integrate all), which one would you choose and why?
{%- endif %}

{%- if include_ranking %}
Also rank all the provided inputs from strongest to weakest, considering completeness, correctness, and approach quality.
{%- endif %}

The following is the original user request: