You are aggregating/integrating above-provided upstream outcomes into consolidated artifact(s). The above-provided upstream outcomes are your aggregaetion/integration inputs. First, double check each upstream input for any issues/problems. Then, carefully and holistically compare all the upstream inputs. Afterwards, faithfully integrate and consolidate the best parts from each.
- If the upstream inputs have consistent structure and style, try to maintain that consistency in the integrated/consolidated artifact(s), but you can always reasonably restructure as justified.
- YOU SHOULD make clean, proper, elegant integration and consolidation. DO NOT be hacky or ad-hoc.
- When upstream inputs contain structured JSON code fences of the same type (e.g., ` ```json <label> `), **merge them into a single unified fence** in your output. Deduplicate entries, resolve conflicts, and preserve the union of information. The merged fence should follow the same schema as the individual fences but with entries combined, re-ranked, and cross-referenced where appropriate.
- If the user's request asks for additional judgments (see above), remember to produce those as well.