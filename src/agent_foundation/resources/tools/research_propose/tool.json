{
  "name": "research_propose",
  "tool_type": "Action",
  "aliases": [
    "research-propose",
    "research",
    "propose"
  ],
  "category": "workflow",
  "is_bridge": true,
  "asynchronous": true,
  "derived_from": {
    "tool": "task",
    "arg_mappings": {
      "workflow_target_path": "request"
    },
    "defaults": {
      "template_master_version": "research_propose",
      "plan": true,
      "config": "breakdown-multiflow-plan",
      "config_overrides": {
        "_params.flow_inferencers": ["${oc.env:DEFAULT_RESEARCH_INFERENCER}", "${oc.env:DEFAULT_MAIN_INFERENCER}"],
        "_params.plan_max_breakdown": 5,
        "_params.flow_max_dynamic_steps": 3,
        "_params.consensus_max_iterations": 3,
        "base_inferencer._template_master_version": "research_propose",
        "base_inferencer.aggregator_inferencer.template_master_version": ["research_propose", "aggregation"]
      }
    },
    "target_path_arg": "workflow_target_path",
    "expose_params": ["--template-version", "--template-master-version"],
    "feed_mappings": {
      "separate_proposal_files": "base_inferencer.aggregator_inferencer.template_extra_feed.separate_proposal_files"
    }
  },
  "description": "Decompose a research question into subtasks, run parallel deep research (literature + internal knowledge), generate code-grounded individual proposals, then synthesize into a unified plan. Supports resumable checkpointing at every stage.",
  "parameters": [
    {
      "name": "request",
      "type": "string",
      "required": true,
      "positional": true,
      "description": "The research task or hypothesis to investigate."
    },
    {
      "name": "--workflow-target-path",
      "type": "path",
      "description": "Path to the codebase or file to research. Falls back to session's target_path if not provided."
    },
    {
      "name": "--docs-path",
      "type": "path",
      "description": "Optional path to documentation directory (included in query and session context)."
    },
    {
      "name": "--separate-proposal-files",
      "type": "flag",
      "description": "Write each proposal's full detail to an individual file (proposals/P1.md, etc.) instead of a single unified file."
    },
    {
      "name": "--research-only",
      "type": "flag",
      "description": "Run breakdown + deep research only (skip individual proposals and unified plan)."
    },
    {
      "name": "--breakdown-only",
      "type": "flag",
      "description": "Run only the task breakdown (decomposition), skip research and proposals."
    },
    {
      "name": "--disable-unified-proposal",
      "type": "flag",
      "description": "Skip the final unified proposal aggregation."
    },
    {
      "name": "--max-breakdown",
      "type": "string",
      "description": "Guidance for how many subtasks to decompose into (e.g., '5 to 20'). Passed to the LLM as a range."
    },
    {
      "name": "--max-researches",
      "type": "int",
      "description": "Hard limit on how many research workers to run. Defaults to all subtasks."
    },
    {
      "name": "--research-inferencer",
      "type": "string",
      "description": "Inferencer type for the research phase (e.g., metamate_cli for literature/knowledge search). Defaults to --base-inferencer."
    },
    {
      "name": "--proposal-inferencer",
      "type": "string",
      "description": "Inferencer type for the proposal phase (e.g., devmate_cli for code reading and actionable proposals). Defaults to --base-inferencer."
    },
    {
      "name": "--base-inferencer",
      "type": "string",
      "description": "Inferencer type for breakdown, proposals, and aggregation (e.g., devmate_cli)."
    },
    {
      "name": "--model",
      "type": "string",
      "description": "Override the LLM model."
    },
    {
      "name": "--resume",
      "type": "path",
      "description": "Resume from a previous workspace. Completed stages are skipped via checkpoints."
    }
  ],
  "returns": "Workspace path containing: breakdown analysis, research reports, individual proposals, and unified plan.",
  "examples": [
    "research-propose --workflow-target-path /path/to/model.py \"Analyze the model for improvement opportunities\"",
    "research-propose --workflow-target-path /path/to/model.py --docs-path /path/to/docs \"Analyze the model and its documentation\"",
    "research --research-only --workflow-target-path /path/to/code --max-researches 5 --research-inferencer metamate_cli \"Investigate optimizations\"",
    "research-propose --resume /path/to/workspace \"Continue from previous research\"",
    "research --breakdown-only --max-breakdown '10 to 30' \"Decompose model improvement opportunities\""
  ]
}
