{
  "name": "task",
  "tool_type": "Action",
  "aliases": [
    "task-plan",
    "task-execute",
    "task-full",
    "task-confirm"
  ],
  "category": "workflow",
  "is_bridge": true,
  "asynchronous": true,
  "executor": "agent_foundation.resources.tools.task.executor:execute",
  "description": "Execute a dual-agent consensus workflow for planning and/or implementation. Two AI agents (proposer + reviewer) iterate until consensus on a plan, then optionally execute it.",
  "parameters": [
    {
      "name": "request",
      "type": "string",
      "required": true,
      "positional": true,
      "description": "The task description or code change request."
    },
    {
      "name": "--plan",
      "type": "flag",
      "description": "Run planning phase only (skip implementation)."
    },
    {
      "name": "--full",
      "type": "flag",
      "default": true,
      "description": "Run full workflow: planning + implementation. This is the default."
    },
    {
      "name": "--execute",
      "type": "flag",
      "description": "Skip planning, execute implementation directly."
    },
    {
      "name": "--confirm",
      "type": "flag",
      "description": "Plan first, then wait for user approval before implementing."
    },
    {
      "name": "--model",
      "type": "string",
      "popular": true,
      "description": "Override the LLM model (e.g., 'opus', 'sonnet', 'claude-opus-4-6').",
      "choices": ["claude-opus-4-6", "claude-sonnet-4-6", "claude-haiku-4-5"]
    },
    {
      "name": "--claude-only",
      "type": "flag",
      "popular": true,
      "description": "Use Claude Code for both proposer and reviewer agents."
    },
    {
      "name": "--analysis",
      "type": "flag",
      "description": "Enable post-implementation analysis phase."
    },
    {
      "name": "--multi-iter",
      "type": "flag",
      "description": "Enable multi-iteration refinement loop."
    },
    {
      "name": "--no-planning",
      "type": "flag",
      "description": "Skip the planning phase entirely."
    },
    {
      "name": "--no-implementation",
      "type": "flag",
      "description": "Skip the implementation phase entirely."
    },
    {
      "name": "--no-aggregate",
      "type": "flag",
      "description": "Run workers but skip aggregation: return the full list of worker outputs instead of one synthesized result (e.g. so a conversational caller can aggregate). Applies to BTA (disable_aggregator) and MFDual (multi_flow_disable_aggregator) nodes."
    },
    {
      "name": "--base-inferencer",
      "type": "string",
      "popular": true,
      "description": "Planner/base agent inferencer type.",
      "choices": [
        "metamate_sdk",
        "devmate_cli",
        "claude_code"
      ]
    },
    {
      "name": "--review-inferencer",
      "type": "string",
      "description": "Reviewer agent inferencer type.",
      "choices": [
        "metamate_sdk",
        "devmate_cli",
        "claude_code"
      ]
    },
    {
      "name": "--resume",
      "type": "path",
      "description": "Resume from an existing workspace directory."
    },
    {
      "name": "--analysis-only",
      "type": "path",
      "description": "Resume workspace and run analysis only (sets --no-planning --no-implementation --analysis)."
    },
    {
      "name": "--analysis-mode",
      "type": "string",
      "description": "Analysis comparison mode.",
      "choices": [
        "last",
        "cross-ref",
        "all-rounds"
      ]
    },
    {
      "name": "--copy-workspace",
      "type": "flag",
      "description": "Force copy workspace on resume."
    },
    {
      "name": "--in-place",
      "type": "flag",
      "description": "Don't copy workspace on resume (modify in place)."
    },
    {
      "name": "--replay-streaming",
      "type": "flag",
      "description": "Replay streamed output from previously completed phases."
    },
    {
      "name": "--use-plan",
      "type": "path",
      "description": "Path to a plan file. Skips the planning phase and goes straight to implementation using this plan."
    },
    {
      "name": "--use-proposal",
      "type": "path",
      "description": "Path to proposals.json from a research-propose run. Selected proposals are formatted into a plan and fed to implementation. Mutually exclusive with --use-plan."
    },
    {
      "name": "--proposal-ids",
      "type": "string",
      "description": "Comma-separated proposal IDs to pick (e.g., 'P1,P3'). Used with --use-proposal. Omit to pick all."
    },
    {
      "name": "--tool-name",
      "type": "string",
      "description": "Workspace identity — controls directory naming under _runtime/tasks/. Default: 'task'. E.g., 'understand_codebase' creates tasks/understand_codebase/understand_codebase_<ts>/."
    },
    {
      "name": "--config",
      "type": "string",
      "description": "Topology preset (default 'default'). 'default'/'pti': full PTI plan+implement. 'breakdown': BTA coverage planning with adaptive per-subtask workers (leaf/breakdown/multiflow). 'multiple'/'multiflow': MFDual diversity, N parallel flows + winner pick. 'full-plan': BTA+MFDual coverage+diversity (alias for breakdown-multiflow-plan). 'disabled'/'conversation': conversational router that picks a preset per request (opt-in). Mode flags (--plan/--execute/--full/--confirm) apply only to 'default'/'pti'; the other presets are plan-only. Also accepts a YAML path or inline YAML.",
      "choices": [
        "default",
        "breakdown",
        "multiple",
        "full-plan",
        "disabled",
        "conversation",
        "pti"
      ]
    },
    {
      "name": "--override",
      "type": "string",
      "multi": true,
      "description": "YAML config override as key=value. Repeatable. E.g.: --override '_params.default_inferencer=RovoDevCLI' --override '_params.plan_max_breakdown=4'"
    },
    {
      "name": "--template-version",
      "type": "string",
      "description": "Template variant version suffix for variable resolution (e.g., 'aggregation')."
    },
    {
      "name": "--template-master-version",
      "type": "string",
      "description": "Template master version for subdirectory routing (e.g., 'understand_codebase')."
    }
  ],
  "returns": "Workspace path containing plan and/or implementation outputs at _rankevolve_workspace/task_<timestamp>/.",
  "examples": [
    "task \"Implement flash attention v2 for HSTU model\"",
    "task --plan \"Add unit tests for the recommendation service\"",
    "task --analysis --resume /path/to/workspace",
    "task --confirm \"Refactor the data pipeline to use async I/O\""
  ]
}
